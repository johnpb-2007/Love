import { Body } from "@/components/ui";

export function Footer() {
  return (
    <footer className="section-padding !py-10 text-center">
      <Body className="text-sm text-paper-cream/60">
        Made with ❤️ — every scroll hides another surprise.
      </Body>
    </footer>
  );
}
